<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0D14">

    <!-- NavHost — занимает всё пространство над BottomNav -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/navHostFragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottomNavigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Bottom Navigation Bar -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#0F1219"
        app:itemIconTint="@drawable/selector_nav_icon"
        app:itemTextColor="@drawable/selector_nav_text"
        app:menu="@menu/menu_bottom_nav"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:elevation="0dp" />
    <!--
        Привязка в MainActivity.onCreate:
        val navController = findNavController(R.id.navHostFragment)
        bottomNavigation.setupWithNavController(navController)
        // Скрывать BottomNav на splash:
        navController.addOnDestinationChangedListener { _, dest, _ ->
            bottomNavigation.isVisible = dest.id != R.id.splashFragment
        }

        selector_nav_icon.xml и selector_nav_text.xml — создать по аналогии
        с selector_chip_background: selected=#F5A623, default=#3D4455
    -->

</androidx.constraintlayout.widget.ConstraintLayout>