<?xml version="1.0" encoding="utf-8"?>
<!--
    Анимация появления текстовой группы (delay = 200ms)
    Применить: text_group.startAnimation(
                   AnimationUtils.loadAnimation(context, R.anim.anim_splash_text_enter))
-->
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:fillAfter="true"
    android:shareInterpolator="false">

    <!-- Alpha с задержкой 200ms -->
    <alpha
        android:fromAlpha="0.0"
        android:toAlpha="1.0"
        android:duration="600"
        android:startOffset="200"
        android:interpolator="@android:anim/decelerate_interpolator" />

    <!-- Scale с задержкой 200ms + overshoot -->
    <scale
        android:fromXScale="0.8"
        android:fromYScale="0.8"
        android:toXScale="1.0"
        android:toYScale="1.0"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="600"
        android:startOffset="200"
        android:interpolator="@android:anim/overshoot_interpolator" />

</set>