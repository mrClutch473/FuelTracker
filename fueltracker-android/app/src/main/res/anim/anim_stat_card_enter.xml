<?xml version="1.0" encoding="utf-8"?>
<!--
    Анимация появления Summary-карточек: fadeIn + translateY вверх.
    Применять последовательно в onViewCreated с stagger-задержкой:

    val cards = listOf(cardAvgConsumption, cardBestConsumption, cardTotalSpent, cardTotalLiters)
    cards.forEachIndexed { i, card ->
        card.alpha = 0f
        card.translationY = 40f
        card.animate()
            .alpha(1f)
            .translationY(0f)
            .setDuration(400)
            .setStartDelay(i * 80L)
            .setInterpolator(DecelerateInterpolator())
            .start()
    }
-->
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:fillAfter="true">

    <alpha
        android:fromAlpha="0.0"
        android:toAlpha="1.0"
        android:duration="400"
        android:interpolator="@android:interpolator/decelerate_quad" />

    <translate
        android:fromYDelta="8%p"
        android:toYDelta="0"
        android:duration="400"
        android:interpolator="@android:interpolator/decelerate_quad" />

</set>