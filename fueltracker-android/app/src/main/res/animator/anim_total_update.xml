<?xml version="1.0" encoding="utf-8"?>
<!--
    "Пульсация" tvTotal при пересчёте суммы: scale 1.0 → 1.08 → 1.0, 200ms.
    Apply (Kotlin, вызывать при каждом изменении etLiters / etPrice):
        val set = AnimatorInflater.loadAnimator(context, R.animator.anim_total_update) as AnimatorSet
        set.setTarget(tvTotal)   // scaleX
        // scaleY анимируется вторым objectAnimator в том же <set>
        set.start()
    Для одновременной анимации scaleX и scaleY используй AnimatorSet.playTogether() в коде,
    загрузив этот файл дважды с разными target-свойствами, или применяй как написано ниже
    через android:ordering="together".
-->
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:ordering="together">

    <objectAnimator
        android:duration="100"
        android:interpolator="@android:interpolator/fast_out_slow_in"
        android:propertyName="scaleX"
        android:repeatCount="1"
        android:repeatMode="reverse"
        android:valueFrom="1.0"
        android:valueTo="1.08"
        android:valueType="floatType" />

    <objectAnimator
        android:duration="100"
        android:interpolator="@android:interpolator/fast_out_slow_in"
        android:propertyName="scaleY"
        android:repeatCount="1"
        android:repeatMode="reverse"
        android:valueFrom="1.0"
        android:valueTo="1.08"
        android:valueType="floatType" />

</set>